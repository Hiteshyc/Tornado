import AuthButton from "../components/AuthButton";

/**
 * Dashboard page — placeholder.
 * The Report button is globally rendered via layout.tsx (fixed bottom-right).
 * Full dashboard UI will be built separately.
 */
export default function DashboardPage() {
  return (
    <div>
      <AuthButton />
    </div>
  );
}
